# 2026-09-10 — Sabija latent-seed residual risk

Surface recall/fluency can look fine while sleep fragmentation, activity drop, and speech-pause residual still carry *seeds* of decline (Yoga Sutra I.45–46).

```
python3 gvu_sabija_seed.py
```

Verifier: latent_w in [0.45, 0.80] and if latent_seed >= 0.55 band cannot be low.
Smoke: pass1 low 0.291 (w=0.20) → pass2 medium 0.517 (w=0.70) verified.
